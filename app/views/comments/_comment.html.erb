<p><strong><%= comment.commenter %></strong> said: <%= comment.body %></p>
 
<p>
	<% if user_signed_in? %>
    <%= link_to 'Delete', [comment.article, comment],
      method: :delete,
      data: { confirm: 'Are you sure?' }, class: "btn btn-primary" %>
  <% end %>
</p>

<hr>